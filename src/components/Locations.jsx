import React from 'react';

export default function Locations(props) {
    return (
        <div>
            <h1>Locations Page</h1>
            <p>This is where the expandable location cards will go.</p>
        </div>
    );
}